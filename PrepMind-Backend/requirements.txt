# PrepMind Backend Dependencies

# ── Web Framework ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9      # For file uploads (audio, images)
pydantic==2.9.0              # Data validation
python-dotenv==1.0.1         # Load .env file

# ── AI / LLM ───────────────────────────────────────────────────────
groq==0.11.0                 # Groq LLM (MCQ, doubts, planner)
google-generativeai==0.8.3   # Gemini Vision (answer evaluation)
langchain==0.3.7             # LLM chaining + RAG pipeline
langchain-groq==0.2.1        # Groq integration for LangChain
langgraph==0.2.35            # Study planner agent orchestration

# ── Vector Database / Embeddings ───────────────────────────────────
chromadb==0.5.17             # Local vector store for knowledge base
sentence-transformers==3.2.1 # Local embedding model (free, no API)

# ── Speech-to-Text ─────────────────────────────────────────────────
faster-whisper==1.0.3        # Local Whisper STT (tiny/base model)

# ── Database (Supabase) ────────────────────────────────────────────
supabase==2.9.1              # Supabase Python client

# ── Utilities ──────────────────────────────────────────────────────
httpx==0.27.2                # Async HTTP client
Pillow==10.4.0               # Image processing
aiofiles==24.1.0             # Async file operations
